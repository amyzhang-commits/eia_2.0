# Merged Datasets Output

This folder is used for storing merged/processed datasets generated by the analysis scripts.

## Auto-generated Files

The `scripts/dataset_merger.py` module outputs merged datasets and coverage heatmaps here.

Files in this directory are excluded from version control.
